[master]
${master_pub_ip} ansible_user=${admin_user}

[workers]
%{ for ip in worker_priv_ips ~}
${ip} ansible_user=${admin_user} ansible_ssh_common_args='-o ProxyCommand="ssh -W %h:%p -q ${admin_user}@${master_pub_ip}"'
%{ endfor ~}

[k8s:children]
master
workers
